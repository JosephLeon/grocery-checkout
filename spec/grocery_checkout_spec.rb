require 'spec_helper'

describe GroceryCheckout do
  it 'has a version number' do
    expect(GroceryCheckout::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
